# MoodyAddIn

Update the correct document path in `ThisAddIn.cs` on line `33` to match the path of your local machine

You can login as a user on the Methodology tab
User1: user1@email.com
User2: user2@email.com
all other users: any valid email

Use the document from this repo, as it has the correct bookmarks to enable to content control within the Add-In

If you have difficulty running in Word, go to File->Options->Add-ins->in `Manage` Select `COM Add-ins`->Go and enable the this add in from the checkbox list
